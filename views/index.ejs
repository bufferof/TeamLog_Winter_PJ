<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TeamLog | Quantum Simulation</title>
    <link rel="stylesheet" href="/CSS/style.css" />
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>Quantum Communication Simulator</h1>
            <p class="subtitle">TeamLog Winter Project - BB84 Protocol</p>
        </header>

        <section class="user-info">
            <span class="label">현재 사용자</span>
            <span id="user_id">로딩중...</span>
        </section>
        <section class="card">
            <form id="simulation">
                <div class="form-group">
                    <label for="text_send">메시지</label>
                    <input
                        type="text"
                        id="text_send"
                        name="message"
                        placeholder="보낼 메시지를 입력하세요"
                        required
                    />
                </div>
                <div class="form-group">
                    <label for="target">대상 사용자 ID</label>
                    <input
                        type="text"
                        id="target"
                        name="target_id"
                        placeholder="상대방 ID"
                        required
                    />
                </div>
                <button type="submit" id="submit_btn">
                    양자 키 분배 시작
                </button>
            </form>
        </section>

        <section class="output">
            <h3>시뮬레이션 결과</h3>
            <div id="output_window">아직 결과가 없습니다.</div>
        </section>
    </div>

    <script type="module" src="/SCRIPT/main.js"></script>
</body>
</html>
